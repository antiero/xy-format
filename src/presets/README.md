The idea of this probe:
- large set of presets, under ./presets/

\- corresponding set of projects under ./presetprojs/



the "corresponding projects" are copies of the baseline (which is again bar0.xy, like always. I forgot to include it as ./presetprojs/baseline.xy),

but with their track 1 instrument set to the preset corresponding to their name. (./presetprojs/name.xy corresponds to ./presets/name.preset)



the set of projects is not yet complete, since it is quite labor intensive. I'm going through them alphabetically.

