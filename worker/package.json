{
  "name": "xybuddy-stats-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "npm@11.16.0",
  "engines": {
    "node": ">=24.11.0 <27",
    "npm": ">=11.6.1 <12"
  },
  "scripts": {
    "check": "npm run typecheck && npm test",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "~6.0.2",
    "vitest": "^4.1.9",
    "wrangler": "^4.0.0"
  }
}
